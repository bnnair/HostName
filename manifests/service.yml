apiVersion: v1
kind: Service
metadata:
    name: "k8sdevopscluster"
    labels:
        app: "k8sdevopscluster"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "k8sdevopscluster"
